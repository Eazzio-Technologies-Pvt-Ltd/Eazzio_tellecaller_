PORT=5000
JWT_SECRET=super_secret_jwt_key_for_eazzio_telecaller_system_2026

# Database configuration
# Set DB_TYPE to 'postgres' to use PostgreSQL, or 'sqlite' for a local file-based database
DB_TYPE=sqlite

# PostgreSQL config (used if DB_TYPE=postgres)
DB_HOST=localhost
DB_PORT=5432
DB_USER=telecaller_admin
DB_PASSWORD=admin_password_123
DB_NAME=eazzio_telecaller

# SQLite config (used if DB_TYPE=sqlite)
SQLITE_FILE=./database.sqlite

API_URL=http://localhost:5000